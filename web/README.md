# VibeVoice Web UI

React + Vite + Tailwind skeleton running alongside current app.

## Quickstart
```
cd web
npm install
npm run dev
```
Opens at http://localhost:5173

Configure server URL in code as needed (defaults to http://localhost:8081).

