# VibeVoice Dependencies
# Core ML/AI libraries
torch
accelerate==1.6.0
transformers==4.51.3
diffusers
tqdm
numpy
scipy
huggingface_hub

# Audio processing
librosa
av
aiortc
soundfile

# Vocal isolation (Mel-Band-Roformer)
rotary-embedding-torch
einops

# UI and visualization
gradio>=4.0.0,<6.0.0  # Pin to avoid breaking changes in Gradio 6.0

# Configuration and utilities
ml-collections
absl-py
python-dotenv

# AI services
openai

# Note: Install with: pip install -r requirements.txt
# Or use: pip install -e . (installs from pyproject.toml)
